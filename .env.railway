APP_NAME="Orion App Testing"
APP_ENV=testing
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=https://tu-app.railway.app

# Railway auto-configura estas variables de PostgreSQL
DB_CONNECTION=pgsql
DB_HOST=${PGHOST}
DB_PORT=${PGPORT}
DB_DATABASE=${PGDATABASE}
DB_USERNAME=${PGUSER}
DB_PASSWORD=${PGPASSWORD}

CACHE_STORE=database
SESSION_DRIVER=database
QUEUE_CONNECTION=database

# Generar nueva key para producción
APP_KEY=base64:tu-nueva-key-aqui